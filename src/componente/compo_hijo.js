import React, { Component } from 'react';

class Componentetonto extends Component {
  render() {
    return (
      <span>{this.props.nombre_lista}</span>
    );
  }
}

export default Componentetonto;